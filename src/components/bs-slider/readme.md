# bs-slider



<!-- Auto Generated Below -->


## Overview

A slider for picking a single numeric value (`type="default"`), a value snapped to evenly
spaced ticks (`type="segmented"`), or a min/max pair (`type="range"`) from within a bounded
`min`/`max` range.

## When to use
- A numeric value that's easiest to reason about relative to its bounds (volume, brightness,
  a price range) rather than typed digit-by-digit.
- `type="segmented"` when only a fixed number of discrete stops make sense (e.g. a 5-star
  rating-like scale) but a visual track is still preferable to discrete buttons.
- `type="range"` for picking a min/max pair (e.g. a price range filter) with two thumbs.

## When not to use
- A precise value where mis-dragging by a pixel matters more than the at-a-glance bounds --
  use `bs-input[type="number"]` instead.
- A small, fixed set of mutually exclusive choices -- use a radio group instead.

## Properties

| Property     | Attribute     | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Type                                  | Default     |
| ------------ | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ----------- |
| `ariaLabel`  | `aria-label`  | Accessible name for the slider's native range input(s). Required whenever `showLabel` is `false` (a bare track with no visible "Label" text) -- without it, the internal native `<input>` has no accessible name at all. Setting `aria-label` directly on the `<bs-slider>` host does NOT work for this: that attribute stays on the light-DOM host and is never forwarded into the shadow DOM by the browser. Same pattern/reasoning as `bs-switch`'s and `bs-checkbox`'s identical `ariaLabel` prop. For `type="range"`, this labels the lower-bound thumb; the upper-bound thumb gets `"${ariaLabel} end"` (or `"End value"` when `ariaLabel` is unset). | `string`                              | `null`      |
| `disabled`   | `disabled`    | Disables the slider: sets the native `disabled` attribute on the range input(s), suppresses hover/focus/drag styling and the value tooltip, and prevents interaction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `boolean`                             | `false`     |
| `label`      | `label`       | Label text shown above the track when `showLabel` is set.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `string`                              | `'Label'`   |
| `max`        | `max`         | Maximum selectable value, for all `type`s.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `number`                              | `100`       |
| `min`        | `min`         | Minimum selectable value, for all `type`s.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `number`                              | `0`         |
| `segments`   | `segments`    | Number of equal divisions marked by tick lines along the track, for `type="segmented"` only. The first tick (at the very start of the track) is rendered with `opacity: 0` to keep the tick spacing math consistent while not visually doubling up with the track's own start edge -- this matches the design spec, not a bug.                                                                                                                                                                                                                                                                                                                              | `number`                              | `10`        |
| `showLabel`  | `show-label`  | Shows/hides the "Label" text and editable percentage field(s) above the track, and the static min/max bound labels flanking it. When `false`, only the bare track (and its knob/knobs) renders.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `boolean`                             | `true`      |
| `step`       | `step`        | Step size between selectable values. For `type="segmented"`, the effective step is derived from `(max - min) / segments` instead, so the thumb always snaps to a tick.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `number`                              | `1`         |
| `type`       | `type`        | Which slider shape to render. `range` uses two thumbs (`valueStart`/`valueEnd`); `default` and `segmented` use one thumb (`value`). `segmented` additionally draws `segments` tick marks along the track.                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `"default" \| "range" \| "segmented"` | `'default'` |
| `value`      | `value`       | Current value for `type="default"`/`type="segmented"`. Ignored for `type="range"` -- use `valueStart`/`valueEnd` instead. Mutable so dragging the thumb updates it directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `number`                              | `0`         |
| `valueEnd`   | `value-end`   | Upper thumb's value for `type="range"`. Defaults to `max` when unset. Mutable so dragging the thumb updates it directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `number`                              | `undefined` |
| `valueStart` | `value-start` | Lower thumb's value for `type="range"`. Defaults to `min` when unset. Mutable so dragging the thumb updates it directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `number`                              | `undefined` |


## Events

| Event           | Description                                                                                                                                                                    | Type                                           |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------- |
| `bsChange`      | Emitted when `value` changes via user interaction (dragging the thumb, or committing the value field), with the new value. Only fires for `type="default"`/`type="segmented"`. | `CustomEvent<number>`                          |
| `bsRangeChange` | Emitted when `valueStart`/`valueEnd` changes via user interaction, with both current values. Only fires for `type="range"`.                                                    | `CustomEvent<{ start: number; end: number; }>` |


## Shadow Parts

| Part        | Description                                                            |
| ----------- | ---------------------------------------------------------------------- |
| `"bound"`   | Each static min/max bound label flanking the track itself.             |
| `"control"` | The native range input(s) that drive the knob(s).                      |
| `"field"`   | Each editable percentage-value field, shown top-right above the track. |
| `"fill"`    | The active/filled portion of the track.                                |
| `"label"`   | The "Label" text above the track.                                      |
| `"tooltip"` | The value tooltip shown above a knob while focused/dragging.           |
| `"track"`   | The background (inactive) track.                                       |


## CSS Custom Properties

| Name                               | Description                                                                                                                                                                                                                                                       |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `--bs-slider-bound-color`          | Static min/max bound label color, enabled. Aliased to --bs-text-default.                                                                                                                                                                                          |
| `--bs-slider-bound-color-disabled` | Static min/max bound label color, disabled. Aliased to --bs-text-on-disabled.                                                                                                                                                                                     |
| `--bs-slider-field-bg`             | Percentage field background. Aliased to --bs-input-bg-default.                                                                                                                                                                                                    |
| `--bs-slider-field-border`         | Percentage field border color. Aliased to --bs-input-border-default.                                                                                                                                                                                              |
| `--bs-slider-field-radius`         | Percentage field corner radius. Aliased to --bs-border-radius-100.                                                                                                                                                                                                |
| `--bs-slider-field-text`           | Percentage field text color (input value and "%" suffix). Aliased to --bs-input-text.                                                                                                                                                                             |
| `--bs-slider-field-text-disabled`  | Percentage field text color, disabled. Aliased to --bs-text-on-disabled.                                                                                                                                                                                          |
| `--bs-slider-field-width`          | Width of each editable percentage field. Fixed 56px per the design spec.                                                                                                                                                                                          |
| `--bs-slider-fill-bg`              | Active/filled track background, enabled. Aliased to --bs-surface-action.                                                                                                                                                                                          |
| `--bs-slider-fill-bg-disabled`     | Active/filled track background, disabled. Aliased to --bs-surface-on-action-disabled.                                                                                                                                                                             |
| `--bs-slider-focus-ring-color`     | Knob focus ring color. Aliased to --bs-border-primary-focus.                                                                                                                                                                                                      |
| `--bs-slider-focus-ring-gap-color` | The gap color between the knob and its focus ring (mirrors bs-checkbox's outline-offset gap, drawn via box-shadow here since outline doesn't reliably clip to a circular ::-webkit-slider-thumb/::-moz-range-thumb pseudo-element). Aliased to --bs-surface-base. |
| `--bs-slider-gap`                  | Gap between the label text and the row, and between the row's own items. Aliased to --bs-spacing-100.                                                                                                                                                             |
| `--bs-slider-knob-bg`              | Knob circle background, enabled/active. A solid filled dot per the Figma spec (node 5266:50184) -- not a white circle with a drop shadow. Aliased to --bs-surface-action.                                                                                         |
| `--bs-slider-knob-color-disabled`  | Knob circle background, disabled. Aliased to --bs-surface-on-action-disabled (matches the disabled fill token, not the muted text token).                                                                                                                         |
| `--bs-slider-knob-hit-size`        | Diameter of the knob's invisible hit-target area. Aliased to --bs-spacing-600.                                                                                                                                                                                    |
| `--bs-slider-knob-size`            | Diameter of the knob's visible circle. Aliased to --bs-spacing-300.                                                                                                                                                                                               |
| `--bs-slider-label-color`          | Label text color, enabled. Aliased to --bs-text-default.                                                                                                                                                                                                          |
| `--bs-slider-label-color-disabled` | Label text color, disabled. Aliased to --bs-text-muted.                                                                                                                                                                                                           |
| `--bs-slider-label-font-weight`    | Label text font weight. Aliased to --bs-font-weight-semibold.                                                                                                                                                                                                     |
| `--bs-slider-tick-bg`              | Segmented tick mark color. Aliased to --bs-surface-inset.                                                                                                                                                                                                         |
| `--bs-slider-tooltip-bg`           | Value tooltip background. Aliased to --bs-surface-inverse.                                                                                                                                                                                                        |
| `--bs-slider-tooltip-color`        | Value tooltip text color. Aliased to --bs-text-inverse.                                                                                                                                                                                                           |
| `--bs-slider-tooltip-radius`       | Value tooltip corner radius. Aliased to --bs-border-radius-50.                                                                                                                                                                                                    |
| `--bs-slider-track-bg`             | Inactive track background, enabled. Aliased to --bs-surface-container.                                                                                                                                                                                            |
| `--bs-slider-track-bg-disabled`    | Inactive track background, disabled. Aliased to --bs-surface-action-disabled.                                                                                                                                                                                     |
| `--bs-slider-track-height`         | Track thickness, constant regardless of type. Aliased to --bs-spacing-50.                                                                                                                                                                                         |
| `--bs-slider-track-radius`         | Track corner radius (pill). Aliased to --bs-border-radius-full.                                                                                                                                                                                                   |


----------------------------------------------

*Built with [StencilJS](https://stenciljs.com/)*
